{
  "name": "waiter-node",
  "version": "1.0.0",
  "description": "Service that takes a request and waits",
  "main": "index.js",
  "scripts": {
    "babel-node": "babel-node --presets=env",
    "start": "nodemon --exec npm run babel-node -- ./server.js",
    "build": "babel src -d dist"
  },
  "babel": {
    "presets": [
      "env"
    ]
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "express": "^4.16.3"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babel-preset-stage-0": "^6.24.1",
    "nodemon": "^1.17.3"
  }
}
